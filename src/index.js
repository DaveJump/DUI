import 'core-js/fn/object/assign';
import React from 'react';
import ReactDOM from 'react-dom';
import { Tabs } from './DUI';

// Render the main component into the dom
ReactDOM.render(<Tabs />, document.getElementById('app'));
